import React from 'react'

function history() {
    return (
        <div>
            single history component
            single history component
            single history component
        </div>
    )
}

export default history
